package com.eomcs.lms;
// 56_2: 리스너를 이용하여 스프링 IoC컨테이너 준비하기.
public class App {
  
  // 작업:
  // => ContextLoaderListener 리스너 생성
  // => AppInitServlet의 역할을  ContextLoaderListener로 이전
  // => web.xml변경
  // => AppInitServlet 삭제
  // dummy 클래스
  // =>  기존버전에서 계속 존재했던 클래스라서 그대로 둠!
  // => 단지 버전의 목표에 대한 설명을 기록하기 위해 존재함.
  // => 프로젝트에서 사용되지 않음.
}